import { defineConfig } from 'dumi';

export default defineConfig({
  outputPath: 'docs-dist',
  themeConfig: {
    name: '@onehacker/editable-form',
  },
  publicPath: '/_gdemo/editable-form/',
  base: '/_gdemo/editable-form/'
});
